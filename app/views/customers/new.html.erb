<h1 class="text-2xl font-bold mb-6">
  <%= action_name == "new" ? "Nuevo Cliente" : "Editar Cliente" %>
</h1>

<div class="bg-white p-6 rounded shadow">
  <%= render 'form', customer: @customer %>
</div>

<%= link_to "Volver", customers_path, class: "inline-block mt-4 text-blue-600 hover:underline" %>
