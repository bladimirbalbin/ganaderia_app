<h1 class="text-2xl font-semibold mb-4">Detalle de Usuario</h1>

<div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
  <p><strong>Email:</strong> <%= @user.email %></p>
</div>

<div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
  <p><strong>Rol:</strong> <%= @user.role.name %></p>
</div>

<div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
  <p><strong>Compañía:</strong> <%= @user.company.name %></p>
</div>

<div class="flex space-x-2 mt-4">
  <%= link_to "Editar", edit_user_path(@user), class: "bg-yellow-500 hover:bg-yellow-600 text-white font-semibold py-2 px-4 rounded" %>
  <%= link_to "Volver a usuarios", users_path, class: "bg-gray-500 hover:bg-gray-600 text-white font-semibold py-2 px-4 rounded" %>
</div>
